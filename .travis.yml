language: node_js
node_js:
  - 0.10
before_script:
  - npm update npm -g
notifications:
  irc:
    channels:
      - "irc.freenode.org#ubuntu-si"
    template:
      - "%{repository}/#%{build_number}/%{branch} (%{author}): %{message} %{build_url}"